import React from 'react'
import ReactDOM from 'react-dom/client'
import TaskOrbitApp from './TaskOrbitApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TaskOrbitApp />
  </React.StrictMode>,
)
